{
  "name": "ngx-devkit-stubby-builder",
  "version": "0.0.0",
  "description": "Extensible Builder for the Angular CLI for serve the target application serve task and run and Stubby server for get the control about http requests.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/gpincheiraa/ngx-devkit-stubby-builder"
  },
  "main": "src/index.js",
  "typings": "src/index.d.ts",
  "builders": "builders.json",
  "scripts": {
    "tsc": "tsc",
    "build": "npm run tsc",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@angular-devkit/architect": "^0.7.1"
  },
  "peerDependencies": {
    "rxjs": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^10.3.2",
    "cpr": "^3.0.1",
    "stubby": "^4.0.0",
    "typescript": "^2.9.1"
  }
}
